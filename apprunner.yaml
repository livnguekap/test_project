version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install pipenv
      - pipenv install
      - pipenv install gunicorn

run:
  command: pipenv run manage.py runserver 0.0.0.0:8000

network:
  port: 8000