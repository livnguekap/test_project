version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install django
      - pipenv install gunicorn
run:
  command: |
    pip3 run gunicorn test_project.wsgi:application --log-file -
  network:
    port: 8000
