version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install pipenv
      - pipenv install
      - pipenv install gunicorn

run:
  command: pipenv run gunicorn test_project.wsgi --log-file -
  network:
    port: 8000