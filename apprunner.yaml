version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install pipenv
      - pipenv install django
      - pipenv install gunicorn
run:
  command:
    ./startup.sh

  network:
    port: 8000
