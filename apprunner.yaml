version: 1.0
runtime: python311
build:
  command:
    build:
    - pip install django
run:
  command:
    - python manage.py migrate
    - python manage.py runserver 0.0.0.0:8000"
  network:
    port: 8000
