version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install pipenv
      - pipenv install django
      - pipenv install gunicorn
      - chmod +x startup.sh
run:
  command:
    sudo ./startup.sh

  network:
    port: 8000
